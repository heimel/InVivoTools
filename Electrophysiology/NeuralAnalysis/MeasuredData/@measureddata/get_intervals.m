function inter = get_intervals(measData)

%  GET_INTERVALS(MEASDATA)
%
%  INTER = GET_INTERVALS(MEASDATA)
%
%  Returns the intervals over which the data in the MEASUREDDATA object
%  MEASDATA was measured.
%
%  See also:  MEASUREDDATA

inter = measData.intervals;
