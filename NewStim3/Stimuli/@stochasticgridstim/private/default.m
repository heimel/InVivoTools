function parameters = default
%STOCHASTICGRIDSTIM/DEFAULT
% 2012-2021, Alexander Heimel

if ~exist('rng_twister','file') % for backwards compatibility with R2009
    rs = rand('state'); %#ok<RAND>
else
    rs = rng_twister;
end

parameters = struct ( ...
    'BG', [128 128 128], ...
    'dist', [1;2;1], ...
    'values', [0 0 0; 128 128 128; 255 255 255], ...
    'rect',[100 100 300 300], ...
    'angle', 0,  ...
    'pixSize', [50 50] , ...
    'N',20,...
    'fps',5, ...
    'randState', rs); 
parameters.dispprefs = {};