function g = NewStimList% Part of the NewStim package%%  LIST = NEWSTIMLIST%%  Returns a list of strings containing the names of stimulus objects.%  Should be located in the parent directory of all stimulus object%  classes.%%  Questions to vanhoosr@brandeis.edustimdir = fileparts(which('NewStimList'));g = {};d = dir(stimdir);for i=1:length(d)    if d(i).isdir == 1 & d(i).name(1) == '@' %needed single & for MACOS9        g{end+1} = d(i).name(2:end);    endend